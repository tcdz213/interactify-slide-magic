/**
 * AdminLayout — Main layout for the admin dashboard.
 * Re-exports from components/AppLayout for the new folder structure.
 * The actual implementation stays in components/AppLayout.tsx to minimize
 * import changes across 30+ files that reference sibling components.
 */
export { default } from "@/components/AppLayout";
